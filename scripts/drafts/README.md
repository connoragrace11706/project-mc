# Drafts

Scripts land here. Nothing in this project publishes.

The `PreToolUse` hook in `.claude/settings.json` runs `platform_check.py` on
every write to this directory and **blocks** on fabricated statistics, speed
callouts, engagement bait, and unresolved placeholders. A rule in a skill file
is a request; this is the guarantee.

Naming: `YYYY-MM-DD-<slug>.md`
