.env
*.token
*.secret
api key/
*api*key*.txt

# Per-machine Claude Code overrides. The laptop and the desktop are
# deliberately configured differently - don't sync these.
**/settings.local.json
data/snapshots/**/raw/
footage/
*.mp4
*.mov
*.CR2
*.ARW
Thumbs.db
desktop.ini

# Python env for local transcription (faster-whisper on CUDA). Multi-GB of
# CUDA libs - belongs on disk, never in the repo.
.venv/
__pycache__/
*.pyc
